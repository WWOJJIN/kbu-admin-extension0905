// extension/content.js
// 보조/선택 스크립트 — 핵심 로직 아님 (지침 참고). 협조문 자동 감지의 핵심 역할은
// background.js의 폴링(현재 1분 주기, POLL_PERIOD_MINUTES 참고)이 담당하므로,
// MutationObserver 기반 DOM 감시는 필수가 아니다. F5 시 SPA 상태가 초기화되기
// 때문에 "새로고침 시 새 데이터 감지"를 트리거로 쓰면 안 되고, 폴링 기반이
// 맞는 방향이라 이 파일은 현재 비워둔다.
//
// 필요해지면(예: ERP 화면에 버튼을 얹는 등 DOM 조작이 실제로 필요한 기능이 생기면)
// 여기에 추가한다.
